<html xmlns:v="urn:schemas-microsoft-com:vml"  xmlns:o="urn:schemas-microsoft-com:office:office">
<head>
<hta:application 
    id="oHTA" 
    applicationName="CourseLab"
    singleInstance="yes"
    maximizeButton="no"
    minimizeButton="yes"
    sysMenu="yes"
    scroll="no"
    contextmenu="no"
    selection="no"
    border="thin"
    innerBorder="no"
    navigable="no"/>
<title></title>
<style>
v\:* {behavior:url(#default#VML);}
o\:* {behavior:url(#default#VML);}
</style>
<link href="images/course.css" rel="stylesheet" type="text/css"/>
<script type="text/javascript" src="../courseimages/courselab.js"></script>
<script type="text/javascript" src="../courseimages/lms.js"></script>
<script type="text/javascript" src="../courseimages/legacy.js"></script>
<script type="text/javascript" src="module.js"></script>
<script language="VBScript"> 
Sub agentFSCHub_FSCommand(ByVal command, ByVal args)
Call agentFSCHub_DoFSCommand(command, args)
End Sub
</script>
<script language="JavaScript"> 
function agentFSCHub_DoFSCommand(command, args)
{
TranslateFSCommand(command, args);
return true;
}
</script>

</head>

<body style="margin-left:0px;margin-top:0px;margin-right:0px;margin-bottom:0px;" onload="Run(false)" onunload="Shutdown()">
<script language=JavaScript>
	var nRequiredWidth = 1024;
	var nRequiredHeight = 728;

	var nWidth = nRequiredWidth;
	var nHeight = nRequiredHeight;
	var nLeft = screen.width / 2 - nWidth / 2;
	var nTop = screen.height / 2 - nHeight / 2;

	// Resize window
	window.resizeTo(nRequiredWidth, nRequiredHeight);
	//window.moveTo(nLeft, nTop);

	// Get body size
	var rc = document.body.getBoundingClientRect();
	var nBodyWidth = rc.right - rc.left;
	var nBodyHeight = rc.bottom - rc.top;

	// Calculate window size and position
	nWidth = nRequiredWidth + (nRequiredWidth - nBodyWidth);
	nHeight = nRequiredHeight + (nRequiredHeight - nBodyHeight);
	nLeft = screen.width / 2 - nWidth / 2;
	nTop = screen.height / 2 - nHeight / 2;

	// Resize and position window
	window.resizeTo(nWidth, nHeight);	
	window.moveTo(nLeft, nTop);
	var nRequiredWidth = 1024;
	var nRequiredHeight = 728;

	var nWidth = nRequiredWidth;
	var nHeight = nRequiredHeight;
	var nLeft = screen.width / 2 - nWidth / 2;
	var nTop = screen.height / 2 - nHeight / 2;

	// Resize window
	window.resizeTo(nRequiredWidth, nRequiredHeight);
	//window.moveTo(nLeft, nTop);

	// Get body size
	var rc = document.body.getBoundingClientRect();
	var nBodyWidth = rc.right - rc.left;
	var nBodyHeight = rc.bottom - rc.top;

	// Calculate window size and position
	nWidth = nRequiredWidth + (nRequiredWidth - nBodyWidth);
	nHeight = nRequiredHeight + (nRequiredHeight - nBodyHeight);
	nLeft = screen.width / 2 - nWidth / 2;
	nTop = screen.height / 2 - nHeight / 2;

	// Resize and position window
	window.resizeTo(nWidth, nHeight);	
	window.moveTo(nLeft, nTop);
	var nRequiredWidth = 1024;
	var nRequiredHeight = 728;

	var nWidth = nRequiredWidth;
	var nHeight = nRequiredHeight;
	var nLeft = screen.width / 2 - nWidth / 2;
	var nTop = screen.height / 2 - nHeight / 2;

	// Resize window
	window.resizeTo(nRequiredWidth, nRequiredHeight);
	//window.moveTo(nLeft, nTop);

	// Get body size
	var rc = document.body.getBoundingClientRect();
	var nBodyWidth = rc.right - rc.left;
	var nBodyHeight = rc.bottom - rc.top;

	// Calculate window size and position
	nWidth = nRequiredWidth + (nRequiredWidth - nBodyWidth);
	nHeight = nRequiredHeight + (nRequiredHeight - nBodyHeight);
	nLeft = screen.width / 2 - nWidth / 2;
	nTop = screen.height / 2 - nHeight / 2;

	// Resize and position window
	window.resizeTo(nWidth, nHeight);	
	window.moveTo(nLeft, nTop);
	var nRequiredWidth = 1024;
	var nRequiredHeight = 728;

	var nWidth = nRequiredWidth;
	var nHeight = nRequiredHeight;
	var nLeft = screen.width / 2 - nWidth / 2;
	var nTop = screen.height / 2 - nHeight / 2;

	// Resize window
	window.resizeTo(nRequiredWidth, nRequiredHeight);
	//window.moveTo(nLeft, nTop);

	// Get body size
	var rc = document.body.getBoundingClientRect();
	var nBodyWidth = rc.right - rc.left;
	var nBodyHeight = rc.bottom - rc.top;

	// Calculate window size and position
	nWidth = nRequiredWidth + (nRequiredWidth - nBodyWidth);
	nHeight = nRequiredHeight + (nRequiredHeight - nBodyHeight);
	nLeft = screen.width / 2 - nWidth / 2;
	nTop = screen.height / 2 - nHeight / 2;

	// Resize and position window
	window.resizeTo(nWidth, nHeight);	
	window.moveTo(nLeft, nTop);
	var nRequiredWidth = 1024;
	var nRequiredHeight = 728;

	var nWidth = nRequiredWidth;
	var nHeight = nRequiredHeight;
	var nLeft = screen.width / 2 - nWidth / 2;
	var nTop = screen.height / 2 - nHeight / 2;

	// Resize window
	window.resizeTo(nRequiredWidth, nRequiredHeight);
	//window.moveTo(nLeft, nTop);

	// Get body size
	var rc = document.body.getBoundingClientRect();
	var nBodyWidth = rc.right - rc.left;
	var nBodyHeight = rc.bottom - rc.top;

	// Calculate window size and position
	nWidth = nRequiredWidth + (nRequiredWidth - nBodyWidth);
	nHeight = nRequiredHeight + (nRequiredHeight - nBodyHeight);
	nLeft = screen.width / 2 - nWidth / 2;
	nTop = screen.height / 2 - nHeight / 2;

	// Resize and position window
	window.resizeTo(nWidth, nHeight);	
	window.moveTo(nLeft, nTop);
	var nRequiredWidth = 1024;
	var nRequiredHeight = 728;

	var nWidth = nRequiredWidth;
	var nHeight = nRequiredHeight;
	var nLeft = screen.width / 2 - nWidth / 2;
	var nTop = screen.height / 2 - nHeight / 2;

	// Resize window
	window.resizeTo(nRequiredWidth, nRequiredHeight);
	//window.moveTo(nLeft, nTop);

	// Get body size
	var rc = document.body.getBoundingClientRect();
	var nBodyWidth = rc.right - rc.left;
	var nBodyHeight = rc.bottom - rc.top;

	// Calculate window size and position
	nWidth = nRequiredWidth + (nRequiredWidth - nBodyWidth);
	nHeight = nRequiredHeight + (nRequiredHeight - nBodyHeight);
	nLeft = screen.width / 2 - nWidth / 2;
	nTop = screen.height / 2 - nHeight / 2;

	// Resize and position window
	window.resizeTo(nWidth, nHeight);	
	window.moveTo(nLeft, nTop);
	var nRequiredWidth = 1024;
	var nRequiredHeight = 728;

	var nWidth = nRequiredWidth;
	var nHeight = nRequiredHeight;
	var nLeft = screen.width / 2 - nWidth / 2;
	var nTop = screen.height / 2 - nHeight / 2;

	// Resize window
	window.resizeTo(nRequiredWidth, nRequiredHeight);
	//window.moveTo(nLeft, nTop);

	// Get body size
	var rc = document.body.getBoundingClientRect();
	var nBodyWidth = rc.right - rc.left;
	var nBodyHeight = rc.bottom - rc.top;

	// Calculate window size and position
	nWidth = nRequiredWidth + (nRequiredWidth - nBodyWidth);
	nHeight = nRequiredHeight + (nRequiredHeight - nBodyHeight);
	nLeft = screen.width / 2 - nWidth / 2;
	nTop = screen.height / 2 - nHeight / 2;

	// Resize and position window
	window.resizeTo(nWidth, nHeight);	
	window.moveTo(nLeft, nTop);
	var nRequiredWidth = 1024;
	var nRequiredHeight = 728;

	var nWidth = nRequiredWidth;
	var nHeight = nRequiredHeight;
	var nLeft = screen.width / 2 - nWidth / 2;
	var nTop = screen.height / 2 - nHeight / 2;

	// Resize window
	window.resizeTo(nRequiredWidth, nRequiredHeight);
	//window.moveTo(nLeft, nTop);

	// Get body size
	var rc = document.body.getBoundingClientRect();
	var nBodyWidth = rc.right - rc.left;
	var nBodyHeight = rc.bottom - rc.top;

	// Calculate window size and position
	nWidth = nRequiredWidth + (nRequiredWidth - nBodyWidth);
	nHeight = nRequiredHeight + (nRequiredHeight - nBodyHeight);
	nLeft = screen.width / 2 - nWidth / 2;
	nTop = screen.height / 2 - nHeight / 2;

	// Resize and position window
	window.resizeTo(nWidth, nHeight);	
	window.moveTo(nLeft, nTop);
	var nRequiredWidth = 1024;
	var nRequiredHeight = 728;

	var nWidth = nRequiredWidth;
	var nHeight = nRequiredHeight;
	var nLeft = screen.width / 2 - nWidth / 2;
	var nTop = screen.height / 2 - nHeight / 2;

	// Resize window
	window.resizeTo(nRequiredWidth, nRequiredHeight);
	//window.moveTo(nLeft, nTop);

	// Get body size
	var rc = document.body.getBoundingClientRect();
	var nBodyWidth = rc.right - rc.left;
	var nBodyHeight = rc.bottom - rc.top;

	// Calculate window size and position
	nWidth = nRequiredWidth + (nRequiredWidth - nBodyWidth);
	nHeight = nRequiredHeight + (nRequiredHeight - nBodyHeight);
	nLeft = screen.width / 2 - nWidth / 2;
	nTop = screen.height / 2 - nHeight / 2;

	// Resize and position window
	window.resizeTo(nWidth, nHeight);	
	window.moveTo(nLeft, nTop);
	var nRequiredWidth = 1024;
	var nRequiredHeight = 728;

	var nWidth = nRequiredWidth;
	var nHeight = nRequiredHeight;
	var nLeft = screen.width / 2 - nWidth / 2;
	var nTop = screen.height / 2 - nHeight / 2;

	// Resize window
	window.resizeTo(nRequiredWidth, nRequiredHeight);
	//window.moveTo(nLeft, nTop);

	// Get body size
	var rc = document.body.getBoundingClientRect();
	var nBodyWidth = rc.right - rc.left;
	var nBodyHeight = rc.bottom - rc.top;

	// Calculate window size and position
	nWidth = nRequiredWidth + (nRequiredWidth - nBodyWidth);
	nHeight = nRequiredHeight + (nRequiredHeight - nBodyHeight);
	nLeft = screen.width / 2 - nWidth / 2;
	nTop = screen.height / 2 - nHeight / 2;

	// Resize and position window
	window.resizeTo(nWidth, nHeight);	
	window.moveTo(nLeft, nTop);
	var nRequiredWidth = 1024;
	var nRequiredHeight = 728;

	var nWidth = nRequiredWidth;
	var nHeight = nRequiredHeight;
	var nLeft = screen.width / 2 - nWidth / 2;
	var nTop = screen.height / 2 - nHeight / 2;

	// Resize window
	window.resizeTo(nRequiredWidth, nRequiredHeight);
	//window.moveTo(nLeft, nTop);

	// Get body size
	var rc = document.body.getBoundingClientRect();
	var nBodyWidth = rc.right - rc.left;
	var nBodyHeight = rc.bottom - rc.top;

	// Calculate window size and position
	nWidth = nRequiredWidth + (nRequiredWidth - nBodyWidth);
	nHeight = nRequiredHeight + (nRequiredHeight - nBodyHeight);
	nLeft = screen.width / 2 - nWidth / 2;
	nTop = screen.height / 2 - nHeight / 2;

	// Resize and position window
	window.resizeTo(nWidth, nHeight);	
	window.moveTo(nLeft, nTop);
</script>
<div id="boardFrame" style="position:absolute;left:0px;top:0px;width:1024;height:728">
</div>
<div style="position:absolute; width:100px;height:100px;top:-110px;left:-110px;">
<object  classid="clsid:D27CDB6E-AE6D-11cf-96B8-444553540000" width="100" height="100" id="agentFSCHub">
<param name="Movie" value="../courseimages/agenthub.swf"/>
<param name="WMode" value="transparent"/>
<param name="BGColor" value="#FF9900"/>
<param name="FlashVars" value="objID=agentFSCHub" />
<param name="SWLiveConnect" value="1"/>
<param name="Play" value="-1"/>
<param name="AllowScriptAccess" value="always"/>
</object>
</div>

</body>
</html>
